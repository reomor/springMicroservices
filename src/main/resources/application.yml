catalogs:
  input: /input/
  processed: /processed/

spring:
  batch:
    job:
      enabled: true
  h2:
    console:
      enabled: true
      path: /h2-console
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml